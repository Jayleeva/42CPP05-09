#include "utils.hpp"

int is_over_int_borders(std::string litteral, int len, int type)
{
    std::string max;

    if (type == 0)
    {
        max = "2147483647";
    }
    else if (type == 1)
    {
        max = 
    }
}